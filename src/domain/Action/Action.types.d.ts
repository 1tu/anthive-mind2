declare namespace MAction {
  enum EType {
    STAY = 'stay',
    MOVE = 'move',
    EAT = 'eat',
    LOAD = 'load',
    UNLOAD = 'unload',
  }

  enum EDirection {
    UP = 'up',
    DOWN = 'down',
    RIGHT = 'right',
    LEFT = 'left',
  }
}
