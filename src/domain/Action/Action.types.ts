export interface IAction { }

export enum EDirection {
  UP = 'up',
  DOWN = 'down',
  RIGHT = 'right',
  LEFT = 'left',
}

export enum EName {
  STAY = 'stay',
  MOVE = 'move',
  EAT = 'eat',
  LOAD = 'load',
  UNLOAD = 'unload',
}
